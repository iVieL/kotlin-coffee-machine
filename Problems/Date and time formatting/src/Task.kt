import java.util.*

fun main(args: Array<String>) {
    val scanner = Scanner(System.`in`)

    // write your code here
    print("${scanner.nextInt()}:${scanner.nextInt()}:${scanner.nextInt()} ")
    print("${scanner.nextInt()}/${scanner.nextInt()}/${scanner.nextInt()}")
}
